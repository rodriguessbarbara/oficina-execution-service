apiVersion: v1
kind: Service
metadata:
  name: execution-service
  labels:
    app: execution-service
spec:
  selector:
    app: execution-service
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
